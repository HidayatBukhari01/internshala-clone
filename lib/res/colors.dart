import 'package:flutter/material.dart';

class AppColor {
  static const whiteColor = Colors.white;
  static const subtitle = Color(0xff484848);
  static const cardTitle = Color(0xff333333);
  static const timeTitle = Color(0xff13800d);
  static const buttonBg = Color(0xff00A5EC);
  static const buttonTitle = Color(0xff008bdc);
}
